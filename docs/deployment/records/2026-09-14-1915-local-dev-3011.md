# 本地开发验证：3011 最新 main

| 字段 | 值 |
|---|---|
| 时间 | 2026-09-14 19:15 +0800 |
| 分支 | `feat/client-migrate-user-work`（基于远端 `main`） |
| 模式 | local-development |
| 地址 | `http://127.0.0.1:3011/?topic=T01` |
| 环境变量 | `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL` 已存在；不记录任何值 |
| 状态 | completed |

## 验证

- `npm run typecheck`：通过。
- 首页请求：HTTP 200。
- 使用真实浏览器打开 `127.0.0.1:3011`：React hydration 完成，HMR 连接成功。
- 点击“加入拼桌”：进入“入桌 / 两席 / 碰撞 / 校准 / 第三席”讨论页。
- 使用 `localhost:3011` 打开并点击：同样通过。
- 重新配置 Access Secret 后执行 `auth status --verify`：`verification=valid`，凭据来源为系统 keychain；未记录 Secret。
- 重启 3011 后调用 `/api/discuss`（T19）：HTTP 200，`mode=ai`，`sourceStatus=hybrid`，返回 3 条来源，证明实时知乎结果已接入并可在不足时由当前主题本地语料补齐。

## 根因与修复

开发服务器默认拦截来自 `127.0.0.1` 的 Next HMR/client 资源请求，静态 HTML 能显示但客户端没有完成 hydration，因而按钮没有事件。`next.config.ts` 增加 `allowedDevOrigins: ["127.0.0.1", "localhost"]` 后重启服务，点击流程恢复。

## 说明

本次只验证和修复独立的 3011 main 副本，未迁移 3004 功能，未执行 commit 或 push。开发日志位于 `next-3011-main.out` 与 `next-3011-main.err`。

## 回滚

如需回滚，删除 `allowedDevOrigins` 配置并重启 3011；不影响 3004 项目。
