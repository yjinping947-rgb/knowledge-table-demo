# 部署记录

每次本地生产验证、CI 验证或云端部署都可以在这里留下可复核记录，文件名使用：

```text
YYYY-MM-DD-HHMM-<mode>.md
```

记录至少包含 commit、部署模式、环境变量是否齐全但不包含值、访问地址、健康检查、端到端测试、日志位置、失败原因和回滚动作。严禁写入 API key、Token、密码、Cookie 或完整 secret。

云端部署在没有确定 provider 前只能使用 provider-neutral 记录，不把候选平台写成已上线事实。
