# 本地生产验证：harness 与 skill

| 字段 | 值 |
|---|---|
| 时间 | 2026-09-13 14:56 +0800 |
| 分支 | `chore/harness-skill-retry` |
| 验证 commit | `0aa8337`（验证前，变更尚未提交） |
| 模式 | local-production |
| 地址 | `http://127.0.0.1:3100`（3000 已被占用） |
| 环境变量 | `AI_API_KEY`：本机 mock 测试值存在；`AI_BASE_URL`：指向本机 mock provider；未记录任何真实 secret |
| 状态 | completed |

## 目的

验证本次 harness、四个核心 skill、共享文档入口和 pre-commit hook 改动不会破坏应用构建、生产启动和端到端路径。

## 已完成

- `npm ci`：通过
- `npm run harness:check`：通过
- `npm run lint`：通过，仓库原有 4 个 warning，无 error
- `npm run typecheck`：通过
- `npm run build`：通过
- `PORT=3100 npm run start`：通过，3000 端口被占用后改用 3100
- `curl http://127.0.0.1:3100/`：HTTP 200
- `curl http://127.0.0.1:3100/api/topics`：HTTP 200
- 无真实 AI provider 配置时运行 `BASE_URL=http://127.0.0.1:3100 npm run test:all`：`tests/core-branches.mjs` 通过；`tests/rooms-rag.mjs` 在 RAG 第 4 步失败，原因是缺少 `AI_API_KEY` / `AI_BASE_URL` 时 `/api/answer` fallback 返回空 `retrieved`
- 使用本机 OpenAI-compatible mock provider 后运行 `BASE_URL=http://127.0.0.1:3100 npm run test:all`：core 27 路径通过，RAG 8 路径通过，RAG quality 10/10 通过
- `curl http://127.0.0.1:3100/` 和 `curl http://127.0.0.1:3100/api/topics`：均 HTTP 200

## 收尾

- `tests/rag-quality.mjs` 的话题数量断言已改为从 `src/data/topics.json` 派生，避免新增话题后测试失真
- 开发后需求扫描：没有 open / in-progress 的本地需求记录，`gh` 不可用，因此本次 `no follow-up request`
- 生产服务和本机 mock provider 已关闭
- 日志位置：当前 Codex 终端会话；未写入持久日志文件

## 安全

本记录不保存 Token、密码、Cookie、API key 或完整环境变量值。本机 mock provider 仅用于端到端验证请求链路，不代表真实云端 AI 服务部署。

## 回滚

本地验证失败时关闭服务并回到当前已知可用 commit；代码回滚使用后续提交的 `git revert`，不执行 force push。
