node_modules/
.next/
out/
.env
.env.local
.env.*.local
!.env.example
npm-debug.log*
.DS_Store

# build / cache
tsconfig.tsbuildinfo
next-env.d.ts

# dev test artifacts (next.log / next.out / next.err / next.pid)
next.log
next.out
next.err
next.pid

# agent scratch
.mavis-trash/
.workbuddy/

# large embedding files (regenerate via scripts/embed-topics.mjs)
src/data/topic-embeddings.json
src/data/rag-embeddings.json

# dev / scratch artifacts
.tmp/
*.log
verify-*.txt
embed.log

# local dev server launchers (per-machine, not part of project)
start-devserver*.bat
start-devserver*.sh
